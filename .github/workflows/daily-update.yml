name: Daily Website Update

on:
  schedule:
    # Läuft jeden Tag um 03:00 Uhr morgens (UTC)
    - cron: '0 3 * * *'
  workflow_dispatch: # Erlaubt manuelles Starten auf GitHub

jobs:
  trigger-netlify-build:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Netlify Build Hook
        # WICHTIG: Die URL muss in Anführungszeichen stehen oder sauber übergeben werden,
        # falls sie Sonderzeichen enthält. Das Secret muss im Repo hinterlegt sein!
        run: curl -X POST -d {} "${{ secrets.NETLIFY_BUILD_HOOK_URL }}"